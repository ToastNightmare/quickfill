{
  "installCommand": "pnpm install --no-frozen-lockfile --no-lockfile",
  "buildCommand": "pnpm run build",
  "crons": [
    {
      "path": "/api/cron/health-check",
      "schedule": "0 * * * *"
    }
  ]
}
